# 本地不配置 Supabase 时，后端会自动使用 mock 数据。

# Supabase 项目地址，例如 https://xxxx.supabase.co
SUPABASE_URL=

# 仅后端使用，不要写入前端代码，不要提交真实值。
SUPABASE_SERVICE_ROLE_KEY=

# 如果暂时不用 service role，也可以用 anon key 配合 RLS 策略。
SUPABASE_ANON_KEY=

# Express 服务端口，本地默认 3001，Docker 内默认 80。
PORT=3001
