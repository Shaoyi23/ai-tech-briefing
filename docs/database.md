# 数据库设计

当前 Vite React 版本不启用数据库。

页面数据来自：

```txt
src/data/mock.ts
```

## 后续接入顺序

建议等前端展示和服务器部署稳定后，再按这个顺序恢复数据层：

1. `Feed`：订阅源
2. `Article`：文章
3. `ArticleSummary`：AI 摘要
4. `Bookmark`：收藏
5. `User`：用户系统

## 暂缓原因

当前目标是先上线一个可访问页面，数据库、认证、定时任务和 AI 调用都会增加部署复杂度。等页面稳定后，再逐步接入真实数据会更稳。
